# Daily Log
## 1. Matplotlib Practice

### What I worked on
Spent some time practicing basic plotting with `matplotlib.pyplot`.  
Nothing new conceptually, just reinforcing things by actually writing the code instead of just reading.

### What I did

- Plotted a simple line graph using monthly data (sunny days)
- Used custom labels on the x-axis instead of default indices
- Added title and axis labels to make the plot readable
- Included markers for each data point
- Calculated the average and plotted it using `axhline()`
- Styled the average line a bit (linestyle + color)
- Added a legend to clean things up

### Observations

- I still tend to forget small things like setting labels before calling `legend()`
- Writing the plot step-by-step helps more than trying to do everything in one go
- Even simple styling makes the output look much clearer